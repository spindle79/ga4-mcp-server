# Google Analytics Configuration
GA_PROPERTY_ID=your_ga4_property_id_here
# Path to a Google Cloud service account JSON with the GA4 Data API role.
# The keys/ directory is gitignored — keep your credentials there or anywhere outside the repo.
GOOGLE_APPLICATION_CREDENTIALS=./keys/your-service-account.json

# Server Configuration
PORT=3001
DEBUG=false
